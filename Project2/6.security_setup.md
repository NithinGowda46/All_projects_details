# 🔴 Security Setup

This section documents the AWS Security Groups used to control network access between the components of the Chat Application infrastructure.

AWS Security Groups act as stateful virtual firewalls for associated AWS resources. In this project, Security Groups are used to control access to EC2 servers, EKS resources, ALB, and the database layer. citeturn0search3turn0search2

---

# 🔴 1. Security Group Architecture

The main security boundaries in the project are:

```text
                         Internet / User
                               |
                               v
                        +--------------+
                        |   ALB SG     |
                        |  HTTP / HTTPS|
                        +------+-------+
                               |
                               v
                         +-----------+
                         |    EKS    |
                         | Cluster SG|
                         +-----+-----+
                               |
                 +-------------+-------------+
                 |                           |
                 v                           v
          Application Pods              Backend Pods
                 |                           |
                 +-------------+-------------+
                               |
                               v
                      +------------------+
                      |  DocumentDB SG   |
                      |   DB Port 27017  |
                      +------------------+

          CI Server SG  --->  ECR / AWS Services
          CD Server SG  --->  EKS / AWS Services
```

Security Groups should allow only the traffic required by the application. AWS recommends restricting access to the minimum necessary ports and sources. citeturn0search5turn0search6

---

# 🔴 2. CI Server Security Group

The CI server hosts Jenkins and the tools required for the CI pipeline.

Typical access includes:

- SSH access for administration
- Jenkins access when required
- Outbound access to GitHub
- Outbound access to Amazon ECR and other AWS services

### Security Group Screenshot

> Add the final CI Server Security Group screenshot here.

<!-- <img src="images/ci-server-security-group.png" alt="CI Server Security Group" width="700"> -->

---

# 🔴 3. CD Server Security Group

The CD server is used for Kubernetes administration and Argo CD access through the private network/VPN.

Typical access includes:

- SSH access from the approved administration network
- Kubernetes API access
- Argo CD access through the private network
- Outbound access to AWS services required for deployment

### Security Group Screenshot

> Add the final CD Server Security Group screenshot here.

<!-- <img src="images/cd-server-security-group.png" alt="CD Server Security Group" width="700"> -->

---

# 🔴 4. EKS Cluster Security Group

Amazon EKS uses Security Groups to control communication between the Kubernetes control plane, worker nodes, and other VPC resources. citeturn0search2turn0search8

The cluster Security Group provides the network boundary for EKS control-plane and cluster communication.

### Security Group Screenshot

> Add the final EKS Cluster Security Group screenshot here.

<!-- <img src="images/eks-cluster-security-group.png" alt="EKS Cluster Security Group" width="700"> -->

---

# 🔴 5. Application Load Balancer Security Group

The ALB Security Group controls access to the application entry point.

For the public chat application, the ALB allows the required web traffic from the Internet/CloudFront path.

For the internal Argo CD and Grafana ALB, access is restricted to the private network/VPN CIDR.

### HTTPS Rule

The existing ALB Security Group screenshot is included below.

<img src="images/alb-security-group-https.png" alt="ALB Security Group HTTPS" width="700">

The Security Group should expose only the listener ports required by the ALB configuration.

---

# 🔴 6. DocumentDB Security Group

The DocumentDB Security Group controls access to the database layer.

The database should not be directly exposed to the Internet.

Application traffic should reach DocumentDB only from the approved application/node security boundary and on the required database port.

### DocumentDB Security Group

<img src="images/documentdb-security-group.png" alt="DocumentDB Security Group" width="700">

The database Security Group should follow the principle of least privilege and allow only the required application-to-database traffic. AWS recommends using Security Groups to restrict access to VPC resources and considering Security Groups for Pods when workloads need controlled access to AWS services. citeturn0search0turn0search2

---

# 🔴 7. Security Group Rules Summary

| Security Group | Main Purpose | Expected Access |
|---|---|---|
| CI Server SG | Protect Jenkins/CI server | Administration + required outbound access |
| CD Server SG | Protect deployment server | Administration + EKS/AWS access |
| EKS Cluster SG | Protect EKS cluster communication | Required cluster/control-plane traffic |
| ALB SG | Protect application entry point | Required HTTP/HTTPS traffic |
| DocumentDB SG | Protect database | Database traffic only from approved application sources |

---

# 🔴 8. Security Principles Used

The Security Group configuration follows these principles:

- Allow only required ports
- Restrict administrative access to approved networks
- Avoid unnecessary Internet exposure
- Use Security Group references where appropriate instead of broad CIDR ranges
- Keep database access private
- Separate access boundaries for CI, CD, ALB, EKS, and database resources
- Review inbound and outbound rules before production changes

AWS recommends using Security Groups as the primary mechanism for controlling network access to EC2 resources and restricting traffic to the minimum required. citeturn0search6

---

# 🔴 9. Final Security Architecture

```text
                         Internet
                            |
                            v
                    +----------------+
                    |     ALB SG     |
                    +--------+-------+
                             |
                             v
                    +----------------+
                    |   EKS / App    |
                    |  Cluster SG    |
                    +--------+-------+
                             |
                             v
                    +----------------+
                    | DocumentDB SG  |
                    |    :27017      |
                    +----------------+

       Admin Network / VPN
              |       |
              v       v
        +---------+ +---------+
        | CI SG   | | CD SG   |
        +---------+ +---------+
```

This layered Security Group design helps reduce unnecessary network exposure and provides separate security boundaries for the major AWS components used by the project.

---

# 🔴 10. Security Group Screenshots

The final screenshots for every Security Group used in the project should be maintained in the `Project2/images` directory and referenced from this document.

Recommended screenshot set:

1. CI Server Security Group
2. CD Server Security Group
3. EKS Cluster Security Group
4. Application ALB Security Group
5. Internal ALB Security Group
6. DocumentDB Security Group

These screenshots provide visual evidence of the final inbound and outbound rules configured for the project.